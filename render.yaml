services:
  - type: web
    name: breast-cancer-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app1.main:app --host 0.0.0.0 --port $PORT
